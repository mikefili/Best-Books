<!DOCTYPE html>
<html>
    <head>
        <%- include('../.././layout/head') %>
    </head>
  <body>
      <%- include('../.././layout/header') %>
    <% if (newBook) { %>
      <div class="instruction">Your Book Has Been Added</div>
    <% } %>
   
    <ul>
      <div class="container-xl">
      <li>
        <img src="<%=book.image_url%>" alt="" class="logo" />
        <p class="title">Title is: <%=book.title %></p>
        <p class="author">Author is: <%=book.author %></p>
        <p>Isbn is: <%=book.isbn %></p>
        <p class="description">Description is: <%=book.description %></p>
        <a class="edit" href="/edit/<%= book.id %>">Edit Details</a>
      <form action="/books/<%= book.id %>" method="POST" class="hidden-form">
        <input type="hidden" name="_method" value="DELETE" />
        <button>Delete this book</button>
      </form>
      </li>
     </ul>
    </div>
  </div> 
  </body>
</html>
